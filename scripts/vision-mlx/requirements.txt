# MLX Vision Inference Service Dependencies

# Core
pillow>=10.0.0

# Vision model loading (transformers fallback)
transformers>=4.45.0
torch>=2.2.0
accelerate>=0.28.0

# HTTP server (standard library, no extra deps)

# Optional: MLX when vision support lands
# mlx>=0.20.0
# mlx-lm>=0.20.0
