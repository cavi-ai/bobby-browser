# MLX Vision Inference Service Dependencies

# Core
pillow>=10.0.0
requests>=2.32.0

# Direct local vision inference (canonical mlx-vlm provider)
mlx-vlm>=0.6.0

# Optional: training / fine-tuning
numpy>=1.26.0
