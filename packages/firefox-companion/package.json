{
  "name": "@cavi-ai/bobby-firefox-companion",
  "version": "0.8.0",
  "private": true,
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cavi-ai/bobby-browser.git",
    "directory": "packages/firefox-companion"
  },
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit && esbuild src/background.ts src/content.ts src/popup.ts --bundle --format=iife --platform=browser --outdir=dist && cp manifest.json dist/manifest.json && cp popup.html dist/popup.html && mkdir -p dist/icons && cp -R icons/. dist/icons/",
    "test": "node --import tsx --test test/*.test.ts"
  },
  "devDependencies": {
    "@types/jsdom": "30.0.0",
    "@types/node": "26.2.0",
    "esbuild": "0.28.1",
    "jsdom": "30.0.1",
    "tsx": "4.23.11",
    "typescript": "7.0.2"
  }
}
