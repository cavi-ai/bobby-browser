{
  "name": "@bobby-browser/firefox-companion",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit && esbuild src/background.ts src/content.ts --bundle --format=iife --platform=browser --outdir=dist && cp manifest.json dist/manifest.json",
    "test": "node --import tsx --test test/*.test.ts"
  },
  "devDependencies": {
    "@types/jsdom": "28.0.3",
    "@types/node": "24.10.1",
    "esbuild": "0.28.1",
    "jsdom": "29.1.1",
    "tsx": "4.23.1",
    "typescript": "7.0.2"
  }
}
