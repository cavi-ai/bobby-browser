[package]
name = "node-registry"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[dependencies]
acp-client = { path = "../acp-client", version = "0.8.0" }
auth-broker = { path = "../auth-broker", version = "0.8.0" }
async-trait.workspace = true
config = { path = "../config", version = "0.8.0" }
intent-engine = { path = "../intent-engine", version = "0.8.0" }
thiserror.workspace = true
tracing.workspace = true
types = { path = "../types", version = "0.8.0" }

[dev-dependencies]
tokio.workspace = true
