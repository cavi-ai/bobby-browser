[package]
name = "sdk-core"
version.workspace = true
edition.workspace = true

[dependencies]
serde_json.workspace = true
types = { path = "../types" }
session-manager = { path = "../session-manager" }
page-runtime = { path = "../page-runtime" }
