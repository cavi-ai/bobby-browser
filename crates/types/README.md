# types (internal)

Workspace-only wire types crate (`publish = false`). Source lives in
`crates/bobby-browser-client/src/`.

Public consumers: [`bobby-browser-client`](../bobby-browser-client/README.md).
