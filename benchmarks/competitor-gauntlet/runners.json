{
  "bobby": {
    "description": "bobby-browser MCP (stdio gateway, managed Chromium for repeatability). Runs the default explore toolset \u2014 the standard loop needs no toolset_select since the base-controls batch.",
    "mcpServers": {
      "bobby": {
        "command": "${BOBBY_MCP_COMMAND}",
        "args": [
          "mcp-stdio"
        ],
        "env": {
          "AUTOMATION_RUNTIME_BROWSER_SELECTION": "{\"preference\":{\"mode\":\"exact\",\"engine\":\"chromium\"}}",
          "BOBBY_BROWSER_CONFIG": "${BOBBY_GAUNTLET_CONFIG}"
        }
      }
    }
  },
  "playwright-mcp": {
    "description": "Microsoft @playwright/mcp",
    "mcpServers": {
      "playwright": {
        "command": "npx",
        "args": [
          "-y",
          "@playwright/mcp@latest"
        ]
      }
    }
  },
  "chrome-devtools-mcp": {
    "description": "Google chrome-devtools-mcp",
    "mcpServers": {
      "chrome-devtools": {
        "command": "npx",
        "args": [
          "-y",
          "chrome-devtools-mcp@latest"
        ]
      }
    }
  },
  "raw-playwright": {
    "description": "No agent tooling: bash + the playwright library only",
    "mcpServers": {},
    "promptSuffix": "You have no browser tools. Drive the browser by writing Node scripts that import the 'playwright' package (it is installed in {{harnessDir}}; run scripts with node from that directory, e.g. chromium.launch()) and running them with the Bash tool."
  }
}
